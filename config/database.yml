# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: mysql2
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000
  encoding: utf8

development:
  <<: *default
  database: event_api
  username: root
  password: root

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: event_api_test
  username: root
  password: root

production:
  <<: *default
  database: event_api_prod

#   development:
#   adapter: mysql2
#   database: monsterhunt
#   host: localhost
#   username: root
#   password: root
  

# test:
#   adapter: mysql2
#   database: monsterhunttest
#   host: localhost
#   username: root
#   password: root
#   encoding: utf8
